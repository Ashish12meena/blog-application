package com.bloggera.blog.service.impl;

import org.springframework.stereotype.Service;

import lombok.RequiredArgsConstructor;

@Service
@RequiredArgsConstructor
public class DelegationServiceImpl {
    // public void incrementFollowerCount(String followedUserId) {
    //     userService.incrementFollowerCount(followedUserId);
    // }

    // public void incrementFollowingCount(String loggedUserId) {
    //     userService.incrementFollowingCount(loggedUserId);
    // }

    // public void decrementFollowerCount(String followedUserId) {
    //     userService.decrementFollowerCount(followedUserId);
    // }

    // public void decrementFollowingCount(String loggedUserId) {
    //     userService.decrementFollowingCount(loggedUserId);
    // }

}
